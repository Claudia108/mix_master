<h1><%= @artist.name %></h1>
<p><%= image_tag @artist.image_path %></p>
<p><%= link_to "Edit Artist", edit_artist_path(@artist.id) %></p>
<p><%= link_to "Delete", artist_path(@artist), method: :delete %></p>
